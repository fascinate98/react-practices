import React from 'react'

function Content(){
//   return (
//     <p>04. 함수 컴포넌트 만들기</p>
    
//   );

  return React.createElement('p', null, '04. Pure React(React API)로 컴포넌트 작성하기')
}

export default Content;
